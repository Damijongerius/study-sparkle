/**
 * Store Return Types
 * 
 * These types are re-exported from hooks to maintain consistency.
 * Import them from @/types for centralized type management.
 */

export type { AuthStore } from '@/hooks/useAuth';
export type { StudyStore } from '@/hooks/useStudyStore';

